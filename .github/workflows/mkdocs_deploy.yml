name: MKDocs Deploy
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.x
      - run: |
          pip3 install mkdocs-material
          pip3 install mknotebooks
          pip3 install git-revision-date
      - run: mkdocs gh-deploy --force
